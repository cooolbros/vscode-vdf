{
    "name": "desktop-servers",
    "scripts": {
        "dev": "rspack build --watch --mode none",
        "build": "rspack build"
    },
    "devDependencies": {
        "common": "workspace:*",
        "server": "workspace:*",
        "tsconfig": "workspace:*",
        "vscode-languageserver": "catalog:"
    }
}

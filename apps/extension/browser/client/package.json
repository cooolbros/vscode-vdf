{
    "name": "browser-client",
    "scripts": {
        "dev": "rspack build --watch --mode none",
        "build": "rspack build"
    },
    "devDependencies": {
        "client": "workspace:*",
        "common": "workspace:*",
        "tsconfig": "workspace:*",
        "vscode-languageclient": "catalog:"
    }
}

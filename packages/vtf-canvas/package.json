{
    "name": "vtf-canvas",
    "main": "./pkg/vtf_canvas.js",
    "types": "./pkg/vtf_canvas.d.ts",
    "files": [
        "pkg"
    ],
    "scripts": {
        "dev": "cargo watch -- wasm-pack build --dev --target web",
        "build": "wasm-pack build --release --target web"
    },
    "devDependencies": {
        "vtf": "workspace:^"
    }
}
